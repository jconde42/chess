require "spec_helper"

describe Pawn do
  describe "#move" do
    context "when space infront is empty" do
      xit
    end

    context "when at start" do
      xit
    end

    context "when it can take it piece diagonnaly" do
      xit
    end

    context "when it can use en passent" do
      xit
    end
  end
end
